{
  "name": "aws-webdriver-automation",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "selenium-webdriver": "^4.1.0",
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "mochawesome": "^7.1.3"
  },
  "scripts": {
    "test": "mocha tests --timeout 10000 --reporter mochawesome --reporter-options reportDir=./reports,reportFilename=TestReport,overwrite=false"
  }
}

